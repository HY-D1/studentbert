# Runtime dependencies (verified against actual imports in scripts/ and src/)
# GPU note: install torch as 2.5.1+cu121 on the cluster; cu130 wheels fall back to CPU.
numpy
pandas
scipy
scikit-learn
torch
wandb
matplotlib

# Dev tooling (pre-commit hooks, tests)
ruff
pytest
pre-commit
